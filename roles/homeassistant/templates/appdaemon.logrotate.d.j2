# logrotate script for appdaemon

/var/log/appdaemon/stdout.log /var/log/appdaemon/server.log {
        daily
        missingok
        copytruncate
        rotate 9
        compress
        notifempty
        su root root
        olddir /var/log/appdaemon/old
}
